import { Shifts } from "@/app/shifts/Shifts";

export const metadata = {
  title: "Census | Shifts",
};
const ShiftsPage = () => {
  // render
  // --------------------
  return <Shifts />;
};

export default ShiftsPage;
