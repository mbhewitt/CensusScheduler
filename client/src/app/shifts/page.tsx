import { Shifts } from "@/app/shifts/Shifts";

export const metadata = {
  title: "PEERS | Shifts",
};
const ShiftsPage = () => {
  // render
  // ------------------------------------------------------------
  return <Shifts />;
};

export default ShiftsPage;
