import { Home } from "@/app/Home";

export const metadata = {
  title: "Census | Home",
};
const HomePage = () => {
  // render
  // ------------------------------------------------------------
  return <Home />;
};

export default HomePage;
